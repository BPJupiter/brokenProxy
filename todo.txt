tunnel over ssh:

ssh -w 0:0 ubuntu@nectar

Creates routed IP tunnel. Route DNS traffic through it.
Requires root access...
PermitTunnel yes

ANOTHER OPTION:

sshuttle with DNS forwarding mode:

sshuttle --dns -r user@nectar-instance 0.0.0.0/0


